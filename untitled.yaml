# name of the project
# if init has a project name as an argument, it is used,
# otherwise the name of the parent directory is used
project_name: my awesome project
# name of the docker image to use for the project
# it is always 1 container for each project. 
# If a given project is a multi container application,
# then it will run docker inside a container I guess.
# We will see later...
docker_image: golang:1.6

