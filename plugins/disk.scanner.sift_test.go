package plugins

/*
import (
    "testing"
    "os"
    "io/ioutil"
)

func TestSift(t *testing.T) {
    file, err := ioutil.TempFile(os.TempDir(), "testSiftNut")
    defer os.Remove(file.Name())

    err = Sift("https://raw.githubusercontent.com/matthieudelaro/nutfile_go1.5/master/nut.yml", file.Name())
    if err != nil {
        t.Error(
            err,
        )
    }
}
*/